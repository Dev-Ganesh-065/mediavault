# Backend API URL - change this for production deployment
# For local development, leave as default (Vite proxy will handle it)
# For production (e.g., Vercel), set to your Render/Heroku/Fly.io backend URL
VITE_API_URL=http://localhost:8787